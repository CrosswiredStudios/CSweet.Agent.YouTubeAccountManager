# Jordan Mitchell — YouTube Account Manager

- `portrait-v1.jpg`: agent portrait (768 × 768).
- `csweet-icon.svg`: C-Sweet company logo, copied from the app's `/images/icon.svg`.
- Accent: `#A64B45`.

The root manifest links to these files through this repository's `main` branch.
Those URLs become available after these assets are pushed. The temporary embedded
catalog uses bundled copies so its images and logo work offline. Marketplace
publishing and general offline package asset importing are separate follow-up work.
Keep these assets with the agent when retiring the embedded first-party list.
